server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: on deep-link refresh, return index.html instead of 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
